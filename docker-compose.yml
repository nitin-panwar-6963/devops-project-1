version: '3.9'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      AWS_ACCESS_KEY_ID: 
      AWS_SECRET_ACCESS_KEY: 
      AWS_REGION: 
      DYNAMODB_TABLE: 
    volumes:
      - .:/app
